{
  "name": "smartconnect",
  "version": "1.0.0",
  "description": "SMARTCONNECT — Next-generation learning platform built with the MERN stack",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "seed": "cd backend && node scripts/seedData.js"
  },
  "keywords": [
    "education",
    "portal",
    "mern",
    "gamification"
  ],
  "author": "SMARTCONNECT Team",
  "license": "MIT",
  "devDependencies": {
    "@tailwindcss/aspect-ratio": "^0.4.2",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "mongodb": "^6.20.0"
  }
}
